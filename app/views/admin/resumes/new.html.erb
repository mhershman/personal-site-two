<h4>Add New Resume</h4>

<%= form_for @resume, url: admin_resumes_path do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :image, "Attach an Image" %><br />
  <%= f.file_field :image %><br><br>
  <%= f.submit "Upload Resume", class: "btn" %>
 <% end %>
